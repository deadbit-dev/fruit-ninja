using Interfaces;
using UnityEngine;

namespace Components
{
    public class Spawner : BaseSpawner
    {
        public override void Spawn()
        {
            // TODO: spawn object in random point into boundaries(self pos and scale)
        }
    }
}